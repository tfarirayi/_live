{
    "scripts": {
        "test": "polymer test",
        "dev": "polymer serve | npm run watch",
        "watch": "browser-sync start --proxy 'localhost:3000' --files 'src/*.html, src/*.js, images/*'"
    }
}